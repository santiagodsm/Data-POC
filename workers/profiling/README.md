# Profiling Worker – Skeleton

Background worker that executes profiling jobs queued by the API. For now, this is a folder placeholder. Next steps:

- Choose queue library (RQ or Dramatiq) and connect to Redis.
- Implement a no-op profiling task that produces a placeholder artifact in MinIO.
- Add basic structured logging and job status reporting.

