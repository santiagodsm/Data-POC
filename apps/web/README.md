# Web (React SPA) – Skeleton

Single-page app providing chat UI and context controls. For now, this is a folder placeholder. Next steps:

- Initialize project (Vite + React) and basic routes.
- Add chat view and a simple "Run Profile" action calling the API.
- Display job status and artifact link (from MinIO via the API).

