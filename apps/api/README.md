# API (FastAPI) – Skeleton

Stateless API service to host LangGraph orchestration and endpoints. For now, this is a folder placeholder. Next steps:

- Initialize Python project (pyproject/requirements) and FastAPI app.
- Add `/healthz` and stub endpoints for `/context/*` and `/profiling/run` (enqueue only).
- Integrate DB (SQLAlchemy + Alembic) and Redis queue client.

